<nav class="navbar navbar-expand-lg bg-white navbar-light shadow-sm px-3 px-lg-5 py-3 py-lg-0">
    <a href="/" class="navbar-brand p-0">
        {{#if @site.logo}}
        <img class="site-logo" src="{{@site.logo}}" alt="{{@site.title}}" style=>
        {{else}}
        <h1 class="m-0 text-primary"><i class="fa fa-tooth me-2"></i>{{@site.title}}</h1>
        {{/if}}
    </a>
    <button class="navbar-toggler btn-border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav ms-auto py-0">
            {{#foreach navigation}}
                <a href="{{url}}" class="nav-item nav-link{{#if current}} active{{/if}}">{{label}}</a>
            {{/foreach}}
        </div>
        <button type="button" class="btn text-dark"  data-ghost-search><i
                class="fa fa-search"></i></button>
        <a href="#" onclick="FB.CustomerChat.showDialog(); return false;" class="btn btn-primary py-2 px-4 ms-3">Đặt lịch</a>
    </div>
</nav>